{
  "name": "elder-sign-companion",
  "version": "0.0.1",
  "description": "socket-io server for elder sign companion",
  "main": "main.ts",
  "scripts": {
    "postinstall": "tsc && mv -i -v ./server/public ./server/dist/ && rm -v -rf ./server/src",
    "build": "cd ./server && npm run build",
    "deploy": "git push heroku main",
    "start": "cd server && npm start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
